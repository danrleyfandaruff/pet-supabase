/**
 * @deprecated Removido — todos os CRUD agora passam pelo ApiService → NestJS.
 * Mantido como stub vazio para não quebrar possíveis imports residuais.
 */
export abstract class BaseCrudService<T> {}
